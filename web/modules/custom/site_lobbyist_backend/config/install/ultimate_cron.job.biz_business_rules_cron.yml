langcode: en
status: true
dependencies:
  module:
    - biz_business_rules
title: 'Default cron handler'
id: biz_business_rules_cron
weight: -10
module: biz_business_rules
callback: biz_business_rules_cron
scheduler:
  id: simple
  configuration:
    rules:
      - '0+@ 0 * * *'
launcher:
  id: serial
  configuration:
    timeouts:
      lock_timeout: 3600
    launcher:
      thread: 0
logger:
  id: database
  configuration:
    method: '3'
    expire: 1209600
    retain: 1000
