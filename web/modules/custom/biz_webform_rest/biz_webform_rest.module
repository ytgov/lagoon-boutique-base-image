<?php

/**
 * Implements hook_help().
 */
function biz_webform_rest_help($path, $arg) {
  switch ($path) {
    case 'help.page.biz_webform_rest':
      return 'Allows retrieving webform elements and submitting webforms via REST.';
      break;
  }
}
